import SvgIcon, { TypeIcon } from '../SvgIcon';

const AngleUpIcon = ({ color = '#333', height, width, viewBox }: TypeIcon) => {
  return <SvgIcon>sss {color}</SvgIcon>;
};

export default AngleUpIcon;
